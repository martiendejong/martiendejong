import { ClosestToCenterDirective } from './closest-to-center.directive';

describe('ClosestToCenterDirective', () => {
  it('should create an instance', () => {
    const directive = new ClosestToCenterDirective();
    expect(directive).toBeTruthy();
  });
});
